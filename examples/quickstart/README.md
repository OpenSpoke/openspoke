# examples/quickstart/

Minimum viable OpenSpoke: one hub + one spoke with sample values and dummy
secrets. Follow the guides in
[`docs/installation/`](../../docs/installation/) to build one from scratch.
