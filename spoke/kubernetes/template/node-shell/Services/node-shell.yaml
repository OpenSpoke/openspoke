apiVersion: v1
kind: Service
metadata:
  name: node-shell
  namespace: rag-spoke
spec:
  clusterIP: None
  selector:
    app: node-shell
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
