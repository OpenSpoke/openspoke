// Package version exposes build-time version constants for native-spoke.
package version

// Version is the native-spoke release version reported by --version and
// included in heartbeat payloads.
const Version = "0.0.1"
