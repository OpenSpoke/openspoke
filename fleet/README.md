# fleet/

Standalone Fleet installation and self-managed configuration.

- `manifests/bootstrap/` — day-zero installation onto a fresh hub cluster
- `manifests/managed/` — Fleet self-managed resources (Fleet-managed-by-Fleet)
- `ui/` — Fleet UI custom build (backend proxy + web assets)
