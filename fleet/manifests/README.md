# fleet/manifests/

Fleet CRs and supporting resources.

- `bootstrap/` — initial Fleet install (Namespaces, Secrets, HelmCharts,
  GitRepos, Deployments)
- `managed/` — running Fleet configuration managed by Fleet itself
